import React, { Component } from 'react';

const Navigation = () => {
  return (
    <div>
      Navigation Bar
      <button>Login</button>
      <button>Sign-Up</button>
    </div>
  )
}

export default Navigation;